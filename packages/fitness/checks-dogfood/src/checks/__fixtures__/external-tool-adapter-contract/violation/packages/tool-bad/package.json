{
  "name": "@opensip-cli/tool-bad",
  "version": "0.7.0",
  "private": true,
  "description": "Violation fixture package for the external adapter contract check",
  "dependencies": {
    "@opensip-cli/core": "workspace:*",
    "@opensip-cli/external-tool-adapter": "workspace:*"
  },
  "opensipTools": {
    "kind": "tool",
    "id": "bad",
    "apiVersion": 1,
    "commands": [
      {
        "name": "bad",
        "description": "Bad live view scanner",
        "output": "live-view"
      }
    ],
    "requires": [
      {
        "resource": "filesystem",
        "reason": "Reads project files"
      }
    ],
    "config": {}
  }
}
