/**
 * The current version of the `Tool` plugin contract.
 *
 * ... (abridged)
 *
 * Per ADR-0046 the version is only bumped on actual contract changes and
 * the definition must reference the ADR.
 */
export const TOOL_CONTRACT_VERSION = '1.0.0';
