// Fixture test file (analysis input only; excluded from real test discovery).
// This package has a vitest config, but no coverage thresholds, so the check
// MUST flag the config.
export const sample = 1
