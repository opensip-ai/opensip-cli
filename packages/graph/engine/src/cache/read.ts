/**
 * Cache read (skeleton; implemented in P6).
 *
 * Reads a previously persisted Catalog from disk. Returns null on cache
 * miss (no file, version mismatch, integrity failure).
 */

import type { Catalog } from '../types.js';

export function readCatalog(_catalogPath: string): Catalog | null {
  return null;
}
