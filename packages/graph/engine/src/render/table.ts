/**
 * Table renderer (skeleton; implemented in P4).
 *
 * Renders a Signal[] into a human-readable terminal table.
 */

import type { Renderer } from './types.js';

export const renderTable: Renderer = (_signals, _context): string => {
  throw new Error('renderTable: not implemented (Phase P4).');
};
