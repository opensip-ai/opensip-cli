/**
 * Spinner — re-exported from @opensip-tools/cli-ui so the CLI's
 * static-render path and tool live-views share a single source of truth.
 */

export { Spinner, type SpinnerProps } from '@opensip-tools/cli-ui';
