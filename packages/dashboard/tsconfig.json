{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src"],
  "//exclude": "src/client/** are BROWSER modules (DOM lib) type-checked + bundled separately (src/client/tsconfig.json + scripts/bundle-client.mjs). The Node main build excludes them; it still compiles the generated bundle const (src/client-bundle.generated.ts), which lives outside src/client.",
  "exclude": ["src/client"]
}
