# Customer pnpm workspace

Four small packages model a core domain, an API boundary, a web consumer, and
an unrelated tools-package decoy.
Run package builds recursively with pnpm and execute tests with Vitest.
