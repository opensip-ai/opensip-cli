{
  "name": "@opensip-tools/targeting",
  "version": "3.0.0",
  "license": "MIT",
  "description": "Host file-targeting runtime substrate for OpenSIP Tools — the generic TargetRegistry, uniform glob expansion (resolveTargets/preResolveAllTargets), and globalExcludes filtering any tool consumes via scope.targets",
  "keywords": [
    "opensip-tools",
    "static-analysis",
    "code-quality",
    "targeting",
    "glob"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/opensip-ai/opensip-tools.git",
    "directory": "packages/targeting"
  },
  "homepage": "https://github.com/opensip-ai/opensip-tools",
  "bugs": {
    "url": "https://github.com/opensip-ai/opensip-tools/issues"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@opensip-tools/config": "workspace:*",
    "@opensip-tools/core": "workspace:*",
    "glob": "^13.0.0",
    "minimatch": "^10.2.5"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "typescript": "~6.0.3",
    "vitest": "^4.1.8"
  }
}
