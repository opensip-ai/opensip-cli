# Sample environment file with a (non-real, AWS-documented example) leaked key,
# so a REAL gitleaks run against this fixture project surfaces a finding that
# matches the committed golden. Not a real credential.
DB_HOST=localhost
DB_PORT=5432
APP_ENV=production
LOG_LEVEL=info
REGION=us-east-1
RETRIES=3
TIMEOUT=30
POOL=10
WORKERS=4
CACHE=redis
FEATURE_FLAGS=on
aws_key = AKIAIOSFODNN7EXAMPLE
